# にこまる美容室 ホームページ

Hair & Beauty Salon にこまる美容室の公式ホームページです。
素のHTML/CSS/JavaScriptのみで作られた1ページ構成のサイトで、ビルド作業は不要です。そのままGitHub Pagesなどで公開できます。

## ファイル構成

```
index.html          ページ本体
css/style.css        デザイン(色・レイアウト)
js/main.js           ハンバーガーメニュー・スクロールアニメーションなど
assets/img/logo.png  ロゴ画像
```

## メニュー・料金・物販はGoogleスプレッドシートと自動連携しています

このサイトの「メニュー・料金」「物販」セクションは、以下の2つのGoogleスプレッドシートを自動で読み込んで表示します。
**スプレッドシートの内容を編集するだけで、サイト側は何もしなくても数十秒〜数分で反映されます。**(ファイルの再アップロードは不要です)

- メニュー用シート: https://docs.google.com/spreadsheets/d/1RImllNJFktSgC_on3u8TwsrB2Ml9VY_REjnZurUeWXQ/edit
- 物販用シート: https://docs.google.com/spreadsheets/d/1tUm9vcwxFQcmePR6c52iKO0d6asNkS28m19FpAoTXGs/edit

すでにお使いのGoogleアカウントに作成済みです。以下の2ステップだけ行ってください。

### ステップ1: 共有設定を変更する(最初の1回だけ)

サイトがシートを読み込めるように、それぞれのシートで以下を設定してください。

1. シートを開く
2. 右上の「共有」ボタンをクリック
3. 「全般的なアクセス」を「制限付き」→「**リンクを知っている全員**」に変更
4. 権限は「**閲覧者**」のままでOK
5. 「完了」をクリック

### ステップ2: メニュー・料金・商品を入力する

**メニュー用シート**の列:

| 列名 | 内容 | 例 |
| --- | --- | --- |
| category | カテゴリ名(同じ名前でグループ化されます) | カット |
| name | メニュー名 | カット(シャンプー込み) |
| price | 料金(数字のみ) | 4400 |
| note | 備考(空欄可。名前の後ろに(  )で表示されます) | 学生証提示 |

**物販用シート**の列:

| 列名 | 内容 | 例 |
| --- | --- | --- |
| name | 商品名 | にこまるオリジナルシャンプー |
| price | 価格(数字のみ) | 2200 |
| description | 商品説明 | サロン専売品。しっとりまとまる洗い上がり。 |
| image_url | 商品画像のURL(空欄なら色のプレースホルダーを表示) | |
| note | 備考(空欄可) | |

行を追加・削除・並べ替えすれば、そのままサイトに反映されます。カテゴリ名を新しく追加した場合も自動的に新しいブロックとして表示されます(アイコンは✨が付きます)。

もし読み込みに失敗した場合(共有設定忘れなど)は、`index.html` にもともと書かれている仮の内容がそのまま表示されるので、サイトが壊れて見えることはありません。

### 別のスプレッドシートに切り替えたい場合

`js/main.js` の先頭付近にある `MENU_CSV_URL` と `SHOP_CSV_URL` を、新しいスプレッドシートのURLに書き換えてください(書き換え方は同ファイルのコメントに記載しています)。

## その他、編集してほしい箇所(現在は仮の内容が入っています)

- 住所・電話番号・営業時間 → `index.html` 内の `id="access"` と `id="contact"` のセクション
- スタッフ紹介の名前・コメント → `index.html` 内の `id="staff"` のセクション(画像は現在ロゴを仮置きしているので、実際の写真に差し替え推奨)
- ギャラリー写真 → `index.html` 内の `id="gallery"` のセクション(現在は色のプレースホルダーです)
- Googleマップ → `id="access"` セクションの `<iframe>` の `src` を実際の店舗の地図に差し替え
- LINE ID・Instagramアカウント → `id="contact"` セクション

## GitHub Pagesで公開する方法

1. GitHubのリポジトリページで「Settings」を開く
2. 左メニューの「Pages」を開く
3. 「Build and deployment」の「Source」を「Deploy from a branch」に設定
4. 「Branch」を `main` / `/ (root)` に設定して「Save」
5. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます

## ローカルで確認する方法

`index.html` をブラウザ(Chrome など)にドラッグ&ドロップするだけで見た目を確認できます。
